package com.monumonit.repositories;

import com.monumonit.entities.BioClass;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BioClassRepository extends JpaRepository<BioClass, Long> {
}
